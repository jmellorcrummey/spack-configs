# -------------------------------------------------------------------------
# This is the spack configuration file, CONFIGURED for wp11 on llnl.cs.rice.edu
# Make sure to run bin/spacklink!
# -------------------------------------------------------------------------
modules:
  default:
    enable: [tcl]
    roots:
      tcl:  /home/wp11/hpctoolkit-develop/modulefiles
    tcl:
      include: [hpctoolkit, hpcviewer]
      exclude: ["@:"]
      projections:
        all: '{name}/{version}'
